FROM node:22-alpine AS build

WORKDIR /app
COPY apps/mission-control-ui/package.json ./
RUN npm install
COPY apps/mission-control-ui/ ./
RUN npm run build

FROM caddy:2.9-alpine
COPY deploy/Caddyfile /etc/caddy/Caddyfile
COPY --from=build /app/dist /srv/ui
