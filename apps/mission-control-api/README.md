# Mission Control API

HTTP façade for AEP Mission Control (`/api/v1`).

```bash
AEP_DATA_ROOT=../../var/data \
AEP_BOOTSTRAP_ADMIN_PASSWORD=changeme \
php -S 127.0.0.1:8080 -t public ../../deploy/api-router.php
```

Version: **0.1.0**
